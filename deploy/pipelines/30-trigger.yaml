apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerBinding
metadata: {name: gitea-push, namespace: ai-email-cicd}
spec:
  params:
    - {name: revision, value: $(body.after)}
    - {name: image-tag, value: "pipeline-$(body.after)"}
---
apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata: {name: openclaw-release, namespace: ai-email-cicd}
spec:
  params:
    - {name: revision}
    - {name: image-tag}
  resourcetemplates:
    - apiVersion: tekton.dev/v1
      kind: PipelineRun
      metadata:
        generateName: openclaw-release-
        namespace: ai-email-cicd
        labels:
          app.kubernetes.io/name: openclaw-release
          triggers.tekton.dev/eventlistener: gitea
      spec:
        pipelineRef: {name: openclaw-release}
        taskRunTemplate: {serviceAccountName: pipeline}
        params:
          - {name: git-url, value: "http://gitea.ai-email-cicd.svc.cluster.local:3000/demo/ai-email-demo-app.git"}
          - {name: revision, value: $(tt.params.revision)}
          - {name: image-tag, value: $(tt.params.image-tag)}
        workspaces:
          - name: source
            volumeClaimTemplate:
              spec:
                accessModes: [ReadWriteOnce]
                resources: {requests: {storage: 3Gi}}
          - name: evidence
            persistentVolumeClaim: {claimName: pipeline-evidence}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata: {name: pipeline-evidence, namespace: ai-email-cicd}
spec:
  accessModes: [ReadWriteOnce]
  resources: {requests: {storage: 3Gi}}
---
apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata: {name: gitea, namespace: ai-email-cicd}
spec:
  serviceAccountName: pipeline
  triggers:
    - name: protected-main-push
      interceptors:
        - ref: {name: cel}
          params:
            - name: filter
              value: >-
                header.match('X-Gitea-Event', 'push') &&
                body.ref == 'refs/heads/main' &&
                body.repository.full_name == 'demo/ai-email-demo-app'
      bindings: [{ref: gitea-push}]
      template: {ref: openclaw-release}
---
apiVersion: route.openshift.io/v1
kind: Route
metadata: {name: gitea-webhook, namespace: ai-email-cicd}
spec:
  to: {kind: Service, name: el-gitea}
  port: {targetPort: http-listener}
  tls: {termination: edge, insecureEdgeTerminationPolicy: Redirect}
