# OpenClaw v2 dependency view

This folder is the clean developer-workstation view for the maintained, signed v2 candidate. `package.json` matches the dependency manifest used by the running OpenClaw image.

Use these v1/v2 folders when demonstrating Dependency Analytics, then move to the built-image SBOMs under `sboms/` and the RHACS image results.
